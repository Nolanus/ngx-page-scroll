import { NgxPageScrollDirective } from './ngx-page-scroll.directive';

describe('NgxPageScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPageScrollDirective();
    expect(directive).toBeTruthy();
  });
});
